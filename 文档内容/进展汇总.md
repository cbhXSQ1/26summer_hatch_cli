# Hatch 进展汇总

> **项目**：Hatch — Coding Agent Harness（AI4SE 暑期学校）
> **深度维度**：反馈闭环引擎
> **最后更新**：2026-08-09
> **当前 commit**：`0102ffb`
> **测试**：321/321 通过，81 次提交

---

## 一、项目概述

Hatch 是一个 Python Coding Agent Harness。核心创新是用**客观的反馈信号**（pytest、flake8、mypy 输出）驱动 LLM 自我修正，而非依赖 LLM"自觉"。

### 技术栈

Python 3.10+ / Click CLI / httpx / YAML 配置 / keyring 凭据管理 / DeepSeek+GLM+Claude / prompt_toolkit TUI

---

## 二、完整 Git 提交历史（81 commits，逆序）

### Phase 10：TUI 体验优化与 Agent 机制修复（19 commits，2026-08-06 ~ 08-09）

基于真实使用反馈的迭代修复：日志滚动、滚轮/键盘交互、key 管理、多步工具调用链。

| Commit | 类型 | 说明 |
|--------|------|------|
| `0102ffb` | fix | **轮次 6→12** + 成功轮收尾引导 + 重复命令去重提示 + 观察结果强调不重复执行 |
| `a7ae492` | fix | **工具调用链修复**：纯文本计划（"我先…"）不视为完成，提醒重试；JSON 解析失败不再静默成功；prompt 强化必须输出 JSON 块 |
| `a3a1dea` | fix | **日志切片渲染**：视图=scroll_top 窗口，方向切换任意位置即时响应（根治惰性滚动算法死区） |
| `c58aa4a` | fix | 滚动模型改 scroll_top 锚定，消除方向切换死区 |
| `f58bea6` | fix | 滚轮步长改回 3 行 + Esc timeout 0.1→0.05 |
| `84127a1` | fix | **Esc eager 抢占 emacs pass 绑定**（输入框聚焦也可用）+ 滚轮步长 10 行（后改回 3） |
| `02644aa` | fix | 下拉全宽不透明防混排 + Esc 即时响应 + 滚轮控制层拦截任意位置 + 鼠标模式保活（防切窗失效） |
| `6c15f4e` | fix | 滚轮直接滚动日志 — LogControl 鼠标处理器接管 |
| `c54ed8c` | fix | 日志不设行数上限 + 历史全量加载（滚轮可浏览全部内容） |
| `31dbbf4` | feat | 鼠标滚轮滚动对话日志（保留 PgUp/PgDn） |
| `676c326` | feat | 对话日志滚动浏览 — 工具栏聚焦 ↑↓ 滚动 / PgUp/PgDn 翻页 / 历史可回看 |
| `b6de1df` | fix | **AgentLoop 多步连续调用**：工具成功不退出循环，观察结果回灌下一轮，仅 LLM 收尾算完成 |
| `cf5d996` | fix | DeepSeek 模型列表更新（v4-flash 替换 reasoner）+ 对话历史上限 10→50 提升缓存命中 |
| `e406114` | fix | key 下拉加入布局渲染 + 下拉全宽不透明背景防拼合 |
| `1c8223a` | fix | TUI 过滤流式 json 代码块 + 仅新会话自动命名 |
| `ca1f139` | feat | **key 管理完整流程**：导入显式选模型 + 查看/切换/删除 key + 模型下拉按 key 过滤 |
| `5ca4fd6` | feat | **模型下拉配置驱动** + TUI 导入 provider/key + 切换会话显示历史消息 |
| `0b20771` | fix | 下拉互斥 — 打开一个自动关另一个 + 下拉打开时 Tab 禁用防重叠 |
| `92cd574` | fix | TUI 流式累积不逐字断行 + 工具栏真实焦点切换 + Ctrl+E 编辑器输入兜底 |

### Phase 9：TUI 交互界面（16 commits，2026-08-06）

采用 Subagent-Driven Development：每任务独立子智能体实现 + 审查 + 修复循环，最终整体代码审查后合并。

| Commit | 类型 | 说明 |
|--------|------|------|
| `a5cb273` | fix | 模型切换用新配置构建 LLM（修复终审回归：stale-config） |
| `70c9cf4` | fix | 终审修复：内联重命名 / 空菜单保护 / 日志自动滚动 / 忙时守卫 / 命名状态重置 / LLM temperature kwarg |
| `5cf5021` | test | TUI 冒烟测试（app 构造 + layout 构建，headless 下 patch create_output） |
| `a73d491` | feat | CLI `hatch chat` 命令 |
| `04f6222` | fix | **下拉菜单渲染修复** — FloatContainer 包装（原计划缺陷：dropdown 从未显示） |
| `ab25047` | feat | HatchChatApp 主类（焦点导航 + 下拉菜单 + 异步 agent） |
| `e181a5b` | feat | TUI layout 构建器（对话区 + 输入栏 + 底部工具栏） |
| `91e004b` | feat | SessionManager.rename() |
| `256c4bb` | feat | auto_name() 低温度命名 + LLM.complete 加 temperature 参数 |
| `ed05d64` | fix | 移除合成 round_start 事件 + 修正测试断言（审查发现重复事件 bug） |
| `947884d` | feat | 异步 agent runner（executor 线程 + asyncio.Queue） |
| `4898628` | feat | TUI keybinds（Tab 焦点 / Enter 激活 / Esc 关闭 / 方向键） |
| `b6696f2` | feat | ConversationLog + DropdownMenu widgets |
| `b20424d` | feat | FocusableText widget（焦点高亮） |
| `e6395dc` | feat | TUIEvent dataclasses（8 种事件） |
| `50efb26` | build | prompt_toolkit 依赖 + tui 包骨架 |

### 上一轮对话：交互优化与 bug 修复（12 commits）

| Commit | 类型 | 说明 |
|--------|------|------|
| `52be18a` | fix | 流式文本不装饰 `│` — 中日文字符不被拆碎 |
| `61a7e47` | fix | 流式输出时 thinking 事件字段错误 |
| `53c71e8` | feat | **流式回复** — LLM 输出逐字显示，减少等待感。`LLMBackend.stream()` SSE 生成器，OpenAI/Claude 均支持，MockLLM 每 10 字符模拟 |
| `2243ccd` | fix | **工具轮次也保存助手回复** + 内容上限 2000→8000。修复：工具调用成功后 `context_text` 没赋值，导致对话历史中助手消息丢失，LLM 把连续用户消息当成一条 |
| `a535656` | fix | llm_output 只显示自然语言，滤掉 JSON 代码块 |
| `dd6c3c9` | fix | 纯文本时"思考=回复"一行显示，不冗余标签 |
| `40dfaf5` | fix | 保留思考内容 — 纯文本时 `llm_text` 就是思考 |
| `133a2b1` | fix | **删除重复代码**（导致 `run` 执行两次）|
| `7066b2a` | fix | 默认继续最新会话、**拼接历史对话**、纯文本回复算成功。重写了 SessionManager（去掉了 active_session 文件，改用 `get_latest`），ContextBuilder 支持 `conversation_history` 参数 |
| `f28b850` | fix | 纯文本回复时隐藏空 JSON 块 |
| `651e849` | fix | **重写 system prompt** + 修复 bare `[]` 解析。告诉 LLM：不需要工具时写文本再放空 JSON，永远不要只输出 `[]` |
| `788da4a` | fix | LLM 返回空 JSON 时把文本当回复输出，不再判失败。ActionParser 新增 `extract_text()` + `has_json_block()` |

### 本轮对话：功能开发（4 commits）

| Commit | 类型 | 说明 |
|--------|------|------|
| `3506a55` | feat | **verbose 终端输出** — 实时显示思考/工具调用/结果/反馈/轮次。`AgentLoop.run()` 新增 `on_event` 回调，CLI `_verbose_printer` 格式化打印 |
| `8b9fce1` | feat | **会话管理** — `session new/use/list/info` + `run --cwd` 集成。SessionManager 持久化到 `.hatch/sessions/` |
| `59409e7` | feat | `run` 命令新增 `--cwd` 选项，指定工作目录 |
| `79da3b9` | fix | key set 显示输入字符，只输一次（换回 `input()`） |

### 本轮对话：Bug 修复（2 commits）

| Commit | 类型 | 说明 |
|--------|------|------|
| `c2b2b25` | fix | 修复 key set 在 Windows CMD 无法输入（`getpass` → `click.prompt`） |
| `f32a2da` | test | **全面单元测试覆盖**（+73 tests，160→233） |

### 之前对话：Phase 8（4 commits）

| Commit | 类型 | 说明 |
|--------|------|------|
| `149a17a` | docs | 更新 PLAN.md Phase 8 完成状态 |
| `2234ba4` | feat | T8.5 README.md |
| `9437837` | feat | T8.4 端到端集成测试 + 分类器修复 |
| `0d563ac` | feat | T8.1-T8.3 机制演示（demo_guardrail / demo_feedback / demo_multiround） |

### 之前对话：Phase 7（2 commits）

| Commit | 类型 | 说明 |
|--------|------|------|
| `7983b7e` | feat | T7.2 CLI 入口（click） |
| `bb289e1` | feat | T7.1 AgentLoop 主循环 |

### 之前对话：Phase 5-6（9 commits）

| Commit | 类型 | 说明 |
|--------|------|------|
| `aad43a8` | feat | T6.1 SessionMemory（7 tests） |
| `390c627` | feat | T5.8 FeedbackEngine 集成（4 tests） |
| `2bb505d` | feat | T5.7 FeedbackAggregator（3 tests） |
| `512382e` | feat | T5.6 CorrectionStrategySelector（3 tests） |
| `6d43be8` | feat | T5.5 FailureClassifier（6 tests） |
| `7a6c551` | feat | T5.2-T5.4 反馈解析器（6 tests） |
| `19a663b` | feat | T5.1 测试 Fixtures |
| `23c666a` | feat | T4.2 ContextBuilder（4 tests） |
| `2a2a3c2` | feat | T4.1 ActionParser（6 tests） |

### 之前对话：Phase 0-3（16 commits）

省略 T0.1-T3.3 的早期提交，包含：项目脚手架、数据模型、LLM 适配器、配置加载、凭据管理、6 个工具、4 条护栏规则、HITL。

---

## 三、当前项目结构

```
E:\summerschool\
├── hatch/                               # 源代码（39 Python 文件）
│   ├── cli.py                           # Click CLI（12 命令，含 chat）
│   ├── core/
│   │   ├── models.py                    # 14 dataclass + FailureCategory 枚举
│   │   ├── llm.py                       # LLMBackend + MockLLM + 3 适配器 + stream() + temperature
│   │   ├── loop.py                      # AgentLoop（on_event 回调 + 流式 + 历史上下文）
│   │   ├── parser.py                    # ActionParser（parse + extract_text + has_json_block）
│   │   └── context.py                  # ContextBuilder（system prompt + 历史对话拼接）
│   ├── tools/                           # 6 工具 + ToolRegistry
│   ├── guardrails/                      # 4 规则 + GuardrailChain + HITL
│   ├── feedback/                        # ★ 反馈引擎（parsers→classifier→strategies→aggregator→engine）
│   ├── tui/                             # ★ TUI 交互界面（prompt_toolkit）
│   │   ├── app.py                       # HatchChatApp 主类（焦点导航 + 下拉 + 异步 agent）
│   │   ├── layout.py                    # build_layout（对话区 + 输入栏 + 底栏 + FloatContainer）
│   │   ├── widgets.py                   # FocusableText / ConversationLog / DropdownMenu
│   │   ├── keybinds.py                  # Tab/Enter/Esc/方向键绑定
│   │   ├── agent.py                     # 异步 agent runner（executor + 事件队列）
│   │   ├── events.py                    # 8 种 TUIEvent dataclass
│   │   └── naming.py                    # auto_name() 低温度自动命名
│   ├── memory/
│   │   ├── session.py                   # SessionMemory（键值存储）
│   │   └── session_manager.py           # SessionManager（对话持久化 + 历史 + rename）
│   ├── config/loader.py                 # YAML 配置加载
│   └── security/key_manager.py          # keyring + .env 凭据管理
├── tests/                               # 测试（23 Python 文件，321 测试）
│   ├── test_cli.py                      # CLI 全命令（17 tests）
│   ├── test_llm.py                      # LLM 层（8 类）
│   ├── test_loop.py                     # 主循环
│   ├── test_parser.py                   # ActionParser + extract_text + has_json_block
│   ├── test_tools.py                    # 工具系统（9 类）
│   ├── test_guardrails.py               # 护栏（6 类）
│   ├── test_feedback_parser.py          # 反馈解析器（3 类）
│   ├── test_feedback_classifier.py      # 分类器+策略+聚合器（3 类）
│   ├── test_feedback_loop.py            # 反馈引擎集成
│   ├── test_memory.py                   # 会话记忆
│   ├── test_config.py                   # 配置
│   ├── test_security.py                 # 凭据
│   ├── test_integration.py              # 端到端集成 + TUI 冒烟（8 tests）
│   ├── test_models.py                   # 数据模型
│   ├── test_tui_events.py               # TUI 事件（3 tests）
│   ├── test_tui_widgets.py              # TUI 组件（14 tests）
│   ├── test_tui_agent.py                # 异步 runner（2 tests）
│   ├── test_tui_naming.py               # 自动命名（3 tests）
│   ├── demo_guardrail.py                # 演示 1：护栏拦截
│   ├── demo_feedback.py                 # 演示 2：反馈闭环
│   └── demo_multiround.py               # 演示 3：多轮反馈
├── 文档内容/
│   ├── SPEC.md                          # 完整功能规约
│   ├── PLAN.md                          # 实施计划
│   ├── 用法.md                          # 使用手册
│   └── 进展汇总.md                      # 本文档
├── hatch.yaml                           # 默认配置
├── pyproject.toml
├── README.md
└── .gitlab-ci.yml
```

---

## 四、本轮对话关键改动

### 1. 流式输出（commits `53c71e8` ← `52be18a`）

**改了什么**：
- `LLMBackend` 新增 `stream(messages) -> Generator[str]` 方法
- `OpenAICompatLLM.stream()` 用 SSE 协议（`stream: true`，逐行解析 `data:`）
- `MockLLM.stream()` 每 10 字符 yield 一块
- `AgentLoop` 用 `llm.stream()` 替代 `llm.complete()`，emit `stream_chunk` 事件
- CLI 实时打印每个 chunk（`click.echo(chunk, nl=False)`）

**效果**：LLM 回复逐字出现，不再等一次性返回。

### 2. 对话历史拼接（commit `7066b2a`）

**Bug 现象**：`run` 两次后 LLM 把两条用户消息当成一条（中间有 `\n\n`）

**根因**：工具调用成功后 `state.context_text` 没赋值 → `add_conversation_turn("assistant", ...)` 被跳过 → 对话历史缺少助手回复 → 连续两条 user 消息挨在一起

**修复**（commit `2243ccd`）：工具轮次也保存 LLM 自然语言部分到 `context_text`

### 3. 多次执行 bug（commit `133a2b1`）

**Bug 现象**：一次 `run` 执行两遍

**根因**：CLI `run()` 函数里有一段重复代码（lines 198-232 是 163-197 的完整拷贝），导致 `loop.run()` 被调用两次

### 4. System Prompt 重写（commit `651e849`）

**问题**：LLM 返回空 `[]` 时不输出文本

**修复**：Prompt 明确要求"不需要工具时，先写文本回复再放空 JSON 块，永远不要只输出 `[]`"

### 5. SessionManager 重构（commit `7066b2a`）

**改了什么**：去掉 `config.yaml` 活跃会话文件，改用 `get_latest()`（按 `updated` 排序取最新）。`run` 默认继续最新会话，`session new` 创建新会话。

### 6. 全面测试补充（commit `f32a2da`）

**160 → 247 测试**（+87 tests），覆盖：
- CLI 全命令（17 tests）
- 工具错误路径（13 tests）
- 反馈引擎分支（6 tests）
- 分类器/策略缺口（5 tests）
- Parser 缺口（3 tests）
- SessionMemory 缺口（6 tests）
- HITL/护栏/LLM/Loop 边界（11+5+6 tests）

---

## 五、关键设计决策

| 决策 | 理由 |
|------|------|
| 会话用 `get_latest()` 而非 `active_session` 文件 | 新终端默认继续最新对话，不引入额外状态文件 |
| 流式用 SSE 而非 WebSocket | OpenAPI 兼容的 SSE 更简单，httpx 原生支持 |
| 流式文本不加 `│ ` 装饰 | CJK 字符在 chunk 边界被切碎，且 LLM 回复本身有 markdown 格式 |
| system prompt 放在代码里而非配置文件 | prompt 应和代码逻辑紧密耦合，用户不需要改 |
| `key set` 用 `input()` 显示字符 | 不显示字符的密码输入在 API Key 场景无意义 |

---

## 六、测试覆盖统计

| 模块 | 测试数 | 覆盖类型 |
|------|--------|---------|
| CLI | 17 | 所有命令 + edge cases |
| LLM | 23+ | 抽象基类、Mock、3 适配器、stream、HTTP 错误、temperature |
| Loop | 15+ | 单轮、多轮、HITL、空动作、动作意图检测、JSON 解析失败重试、重复动作去重、多步调用 |
| Parser | 9+ | parse + parse_status + extract_text + has_json_block |
| Tools | 33 | 6 工具 + registry + 所有错误路径 |
| Guardrails | 22+ | 4 规则 + chain + HITL + ABC |
| Feedback | 36+ | 3 parsers + classifier + strategies + aggregator + engine |
| Memory | 12+ | SessionMemory + SessionManager + rename + limit=None |
| Config | 8+ | 加载、校验、默认值、错误、模型列表 |
| Security | 12+ | keyring 操作、mask、.env、provider meta |
| Models | 15+ | 所有 dataclass |
| Integration | 10+ | 端到端 + TUI 冒烟 + 模型切换 + 忙时守卫 + key 流程 + 历史加载 |
| TUI | 30+ | 事件、组件、滚动、滚轮、Esc eager、下拉互斥、命名 |
| Demos | 3 | 护栏拦截 / 反馈闭环 / 多轮反馈 |
| **总计** | **321** | — |

---

## 七、TUI 交互界面（Phase 9 + Phase 10 优化）

`hatch chat [--cwd <dir>]` 启动 prompt_toolkit 交互界面：

```
[对话滚动区 — 切片渲染，滚轮/键盘任意滚动]
──────────────────────────────
> 你的输入...                              ← 固定输入栏（Tab 聚焦后 Enter 提交）
──────────────────────────────
目录  |  会话名  |  More  |  模型  |  key   ← 底部工具栏（Tab 循环聚焦）
```

| 按键 | 行为 |
|------|------|
| `Tab` / `Shift+Tab` | 底栏元素间循环切换焦点（下拉打开时禁用） |
| `Enter` | 提交输入 / 激活当前焦点元素 |
| `Esc` | 关闭下拉 / 取消重命名（eager 抢占，输入框聚焦也立即生效） |
| `↑/↓` | 下拉菜单导航；工具栏聚焦时滚动日志 3 行 |
| `PgUp/PgDn` | 日志翻页 |
| 鼠标滚轮 | 日志任意位置滚动 3 行/格（控制层拦截，含空白区域） |
| `Ctrl+E` | 打开系统编辑器输入（中文 IME 兜底） |

| 焦点 | Enter 行为 |
|------|-----------|
| 会话名 | 内联重命名（输入框预填当前名，再 Enter 确认） |
| More | 下拉列出该目录所有会话，↑↓ 选 + Enter 切换（加载历史消息） |
| 模型 | 下拉按配置 + key 存在性列出模型，热切换（同会话继续） |
| key | 下拉：新增 provider（名称→API地址→模型→key 四步）/ 切换 / 删除 |
| 输入栏 | 提交任务，流式输出逐步追加到对话区 |

关键机制：
- **日志切片渲染**：只渲染可见窗口，视图与 scroll_top 一一对应，方向切换零死区
- **滚动状态**：`scroll_top` + 跟随标志；滚轮/PgUp/↑↓ 共用同一状态机
- **下拉互斥**：同时只开一个，全宽不透明背景防混排
- **鼠标模式保活**：每秒重新启用，防切窗后滚轮失效
- 首次回复后自动调用一次低温度（temperature=0.0）LLM 为会话命名（仅新会话）

---

## 八、运行时流程

```
用户运行: hatch run --cwd D:\project "修复 test.py"

1. CLI 解析 --cwd，os.chdir() 切换目录
2. SessionManager.get_latest_or_create() → 获取或创建会话
3. ConfigLoader.load("hatch.yaml") → 读取 LLM/工具配置
4. KeyManager.get_key() → 从系统凭据管理器读取 API Key
5. _build_llm() / _build_registry() → 构建 LLM 实例 + 工具注册表
6. AgentLoop.run() 启动主循环（多步连续调用，默认 12 轮）：
   ┌─ 第 1/N 轮 ─────
   │ 🧠 思考中...
   │ [流式输出 LLM 回复]
   │ 🔧 调用工具: shell_executor → ✅ 成功
   │ [观察结果回灌下一轮]
   ┌─ 第 2/N 轮 ─────
   │ [LLM 基于上轮观察继续]
   │ 🔧 调用工具: file_reader  → ✅ 成功
   │ [观察结果回灌]
   ┌─ 第 3/N 轮 ─────
   │ [LLM 总结结论 + ```json [] ``` 收尾]
   🎉 任务成功完成 (3 轮)

   完成语义：
   - 工具调用成功 ≠ 任务完成 —— 继续下一轮，观察结果回灌
   - 显式 ```json [] ``` 或"收尾式"纯文本 → 成功
   - 纯文本表达动作意图（"我先…"）→ 提醒格式后重试
   - JSON 解析失败 → 提醒重试（不静默成功）
   - 相同命令重复执行 → 去重提示
7. SessionManager.save/add_conversation_turn → 持久化对话历史
```

---

## 九、CLI 命令一览

```powershell
# 核心
hatch run [--cwd <dir>] [--verbose/--quiet] <任务>   # 执行任务（默认流式详细输出）
hatch chat [--cwd <dir>]                 # 启动 TUI 交互界面（prompt_toolkit）

# 会话
hatch session list [--cwd <dir>]     # 列出所有对话（* 标记最新）
hatch session new  [--cwd <dir>]     # 新建对话
hatch session use  [--cwd <dir>] <id>  # 切换到指定对话
hatch session info [--cwd <dir>]     # 查看当前对话

# 凭据
hatch key set [--provider <name>]    # 录入 API Key（显示输入字符）
hatch key status                      # 查看状态
hatch key clear [--provider <name>]  # 清除
hatch key rotate [--provider <name>] # 替换

# 配置
hatch config show                    # 显示当前配置
hatch config validate                # 验证配置文件
```
